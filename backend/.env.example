ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
PORT=8787
GROQ_API_KEY=
CARTESIA_API_KEY=
# Postmark server token, and the From address it is allowed to send as (must
# be a confirmed sender signature on that Postmark server, or every send comes
# back 401/422). Read by src/data/email.py at call time. Without both, the
# gate's verification step fails closed — nobody gets in.
POSTMARK_API_KEY=
POSTMARK_EMAIL=
