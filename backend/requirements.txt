fastapi>=0.115.0
uvicorn[standard]>=0.30.6
anthropic>=0.34.2
python-dotenv>=1.0.1
# Postmark's REST API is called directly (src/data/email.py). Listed
# explicitly rather than leaned on as a transitive dep of the pipecat/fastapi
# stack — a dependency you import is one you own, whoever else pulls it in.
requests>=2.32.0
pydantic>=2.10.6
pipecat-ai[groq,cartesia,webrtc,silero,runner]>=1.4.0
deepgram-sdk==7.7.0
# CPU-load admission check for the concurrent-calls cap (see server.py's
# _active_call/claim_voice_lock) — psutil.cpu_percent() is what actually
# measures whether the box has headroom for another call.
psutil>=6.0.0
